{
  // The board UI: runs in a browser or a VS Code webview, so it gets DOM types and nothing from Node or VS Code.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client"]
  },
  "include": ["src", "shared"]
}
